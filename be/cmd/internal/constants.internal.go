package internal

// Database name
const (
	dbTemplate = "test"
)

// Redis name
const (
	rdsTest = "test"
)

// Environment constants
const (
	Development = "development"
	Staging     = "staging"
	Production  = "production"
)
